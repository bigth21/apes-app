spring:
  application.name: apes

  datasource:
    url: jdbc:mysql://localhost:3306/apes
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    open-in-view: false



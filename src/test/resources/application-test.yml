spring:
  datasource:
    url: jdbc:mysql://localhost:3307/apes?serverTimezone=UTC&useLegacyDatetimeCode=false
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    open-in-view: false

logging:
  level:
    root: info